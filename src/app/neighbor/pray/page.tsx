import { PraySection } from "@/components/sections/neighbor/PraySection";

export const metadata = { title: "Pray for Someone in Need" };

export default function PrayPage() {
  return <PraySection />;
}
